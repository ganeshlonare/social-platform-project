spring:
  application:
    name: connection-service
  neo4j:
    uri: bolt://neo4j:7687
    authentication:
      username: neo4j
      password: ganesh123

server:
  port: 8083
  servlet:
    context-path: /connections

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
